{
  "name": "gdelt-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "Read-only MCP server for global news intelligence using the GDELT DOC 2.0 API",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check api/mcp.js && node --check api/health.js && node --check lib/gdelt.js",
    "test": "node --test tests/*.test.js",
    "build": "npm run check"
  }
}
